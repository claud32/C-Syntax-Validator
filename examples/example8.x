int func() {
  unsigned char anotherFunc(float a) {
    a = a - 1;
    return a;
  }
  return 3 + 4;
}

unsigned long someFunc(double b, unsigned short c) {
  b = (4);
  b = anotherFunc(b);
}

void main() {
  if (b < 3) {
    while (b < 3) {
      b = b + 1;
      if (b >= -1) {
        return 0;
      }
    }
  }
}
